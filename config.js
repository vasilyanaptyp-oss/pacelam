// Paceļam configuration. Leave SUPABASE_URL empty to run the built-in demo mode (no backend).
// The anon key is public by design: every row a browser can read is decided by database policies.
window.PACELAM_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  PHOTO_BUCKET: 'photos',
};
